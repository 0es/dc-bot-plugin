// ── Plugin defaults ───────────────────────────────────────────────────────────

export const DEFAULTS = {
  /** Default node-worker URL for single-bot / fallback mode. */
  workerUrl: "http://127.0.0.1:3000",
} as const;

export const PLUGIN_ID = "gami-discord-recruit";
